import prisma from '../connection';

export const userModel = prisma.user;
export const paymentModel = prisma.payment;
export const projectModel = prisma.project;
export const documentationModel = prisma.documentation;
export const fileModel = prisma.file;
